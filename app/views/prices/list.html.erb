<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <%= link_to "Далее", list_price_path(scan: @sscan_result[0]) %>
      <br />
      <br />

      <% @results.each do |result| %>
        <pre><%= result %></pre>
      <% end %>
    </div>
  </div>
</div>
